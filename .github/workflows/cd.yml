name: CD Pipeline

on:
  pull_request:
    types: [closed]
    branches: [main]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  # ============================================================================
  # JOB 1: DEPLOY TO PRODUCTION (SERVER 1)
  # ============================================================================
  # Only runs if PR was successfully merged to main
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true

    steps:
      - name: Debug - Test connectivity to Server 1
        run: |
          echo "Testing TCP connectivity to Server 1 on port 22..."
          echo "Target host: ${{ secrets.SERVER1_HOST }}"
          nc -zv -w 10 ${{ secrets.SERVER1_HOST }} 22 || echo "TCP connection FAILED"
          echo "---"
          echo "Runner public IP:"
          curl -s https://api.ipify.org
          echo ""

      - name: Deploy to Server 1
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.SERVER1_HOST }}
          username: ${{ secrets.SERVER1_USERNAME }}
          key: ${{ secrets.SERVER1_SSH_KEY }}
          script: |
            set -e
            echo "Starting deployment to Server 1..."

            # Authenticate with GitHub Container Registry
            echo "Logging in to GHCR..."
            echo "${{ secrets.GHCR_PAT }}" | docker login ghcr.io -u ${{ secrets.GHCR_USERNAME }} --password-stdin

            # Navigate to deployment directory
            cd ~/travery-deployment

            # Pull the latest image
            echo "Pulling latest Docker image from GHCR..."
            docker compose -f docker-compose-prod.yml pull app

            # Restart only the app container (Nginx, DB, Redis, Alloy stay running)
            echo "Restarting app container..."
            docker compose -f docker-compose-prod.yml up -d app

            # Clean up unused images
            echo "Cleaning up old images..."
            docker image prune -f

            echo "Deployment complete!"

            # Verify deployment
            docker ps | grep travery-app && echo "✓ App container is running"
